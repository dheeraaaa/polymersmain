# Optional for Docker Compose: it will build without this file. Required before the API can connect to Atlas.
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@cluster0.51ybx5h.mongodb.net/stockai?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=replace-with-a-random-secret-at-least-32-characters
CORS_ORIGIN=http://localhost:3000
BACKEND_PORT=4000
